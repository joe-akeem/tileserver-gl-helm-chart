{{- if .Values.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tileserver-gl-helm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "tileserver-gl-helm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tileserver-gl-helm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
{{- end }}
