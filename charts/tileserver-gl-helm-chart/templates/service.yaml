{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tileserver-gl-helm-chart.fullname" . }}
  labels:
    {{- include "tileserver-gl-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tileserver-gl-helm-chart.selectorLabels" . | nindent 4 }}
{{- end }}
